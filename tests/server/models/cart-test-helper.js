describe('Cart model', function() {
	describe('methods', function() {
		describe('#merge', function() {
			xit('throws error when the two carts have different users')
			xit('takes products from other cart and pushes them onto this cart')
			xit('sums the quantity if both carts have the smae product')
			xit('removes other cart from db')
		})
	})
})